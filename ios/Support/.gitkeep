XcodeGen writes Info.plist and App.entitlements into this directory.
Git does not track empty directories, and project.yml points INFOPLIST_FILE and
CODE_SIGN_ENTITLEMENTS here, so this file must exist for a fresh CI checkout to build.
