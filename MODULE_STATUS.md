# Module status

Honest per-module status. REAL = compiles and does what it claims. PARTIAL = compiles,
does part of what it claims, rest is a labelled stub. STUB = typed placeholder behind
the real contract, clearly labelled, never faked as working. One row per module, one
line of detail. Appended by each module's own agent; do not edit another module's row.

| Module | Status | Detail |
|---|---|---|
| ci-bottle (.github/workflows/ios.yml, BOTTLE_HOOKS.md) | REAL | XcodeGen + xcodebuild archive (CODE_SIGNING_ALLOWED=NO/CODE_SIGNING_REQUIRED=NO/empty CODE_SIGN_IDENTITY/DEVELOPMENT_TEAM/PROVISIONING_PROFILE_SPECIFIER, no Rust step) hand-zipped (`Payload/<NIMBUS_PRODUCT_NAME>.app` -> `.ipa`, product name discovered by globbing `Products/Applications/*.app`, never hardcoded) to a real unsigned .ipa on macos-15, uploaded every run (`actions/upload-artifact@v4`) + attached to a GitHub Release on `v*` tags (`softprops/action-gh-release@v2`, `permissions: contents: write`), `::warning::` above 512 MB (536870912 bytes, `stat -f%z`); independent `booster-check` job on ubuntu-latest byte-compiles (`python -m compileall`) + lints (`python -m pyflakes`) `booster/`, failing loudly with a named error if `booster/` is missing rather than silently skipping. SECOND PASS (2026-09-03): booster/ has now landed (src/nimbus_booster/{brand,protocol/wire,server/paths}.py) since this module's prior pass, which had only the "booster/ does not exist yet" case to reason about abstractly — this pass mechanically re-verified the whole workflow instead of trusting the prior claims: (1) parsed `ios.yml` with PyYAML (`yaml.safe_load`) to confirm it is structurally valid YAML with the exact two jobs `build`/`booster-check` on `macos-15`/`ubuntu-latest`; (2) extracted every `run:` block from every step and passed each through `bash -n` (syntax-only, no execution) — all 9 scripts across both jobs parse clean; (3) actually ran this machine's Python 3.12 + a locally installed `pyflakes` 3.4.0 with the exact commands the `booster-check` job runs (`python -m compileall -q booster` then `python -m pyflakes booster`) against the real, now-landed `booster/` tree — both exit 0 today, so the job is not just syntactically fine but would currently pass for real. No macOS/Xcode/GitHub Actions runner available here, so the `build` job itself (XcodeGen + xcodebuild archive) has still never actually executed — that remains the one real gap, and depends on the Swift under `ios/Sources/*` compiling, which is those modules' responsibility. `_superseded/app-brush-wrapper/.github/workflows/ios.yml` (the stale Rust-era duplicate) was confirmed still not at the repo root, so left in place as instructed — it cannot be picked up by GitHub Actions from there. BOTTLE_HOOKS.md's source.json `apps[]` entry (bundle id `com.tombline.nimbus` read straight from `BrandConfig`/`project.yml`'s brand block, repo `bottlemaster-22/Nimbus3D`) was re-checked against the current `project.yml`/`BrandConfig.swift` and still matches exactly; `downloadURL` stays a placeholder (`releases/latest/download/Nimbus3D.ipa`) until the first `v*` tag actually publishes a release asset. |
| blender-addon (blender_addon/) | REAL (.ply import, geometry-nodes/shader render) / PARTIAL (.spz decode) | Blender 4.x add-on, single-constant product name (brand.py). Generic PLY header parser (no assumed property layout) + INRIA/gsplat-convention splat attribute mapping; Python-built Geometry Nodes group instances oriented, per-axis-scaled ico-spheres (disc/ellipsoid approximation, SH degree-0 colour only) + unlit alpha-blended shader. Verified for real, reproducibly: `blender_addon/selftest.py` is checked into the repo and was just re-run headless against Blender 5.2.0 LTS (installed on this machine, `C:\Program Files\Blender Foundation\Blender 5.2\blender.exe --background --factory-startup --python selftest.py`) -- registers the real add-on package, builds synthetic .ply/.spz files by hand from each format's documented byte layout, imports them through the real bpy.ops operator, evaluates the Geometry Nodes modifier through the depsgraph and checks the resulting mesh's vertex/face counts against the expected ico-sphere instancing, checks the material is on the *evaluated* mesh (a Geometry-Nodes Set-Material assignment doesn't populate object.data.materials) and its node graph, checks re-import doesn't duplicate datablocks, checks a plain non-Gaussian point-cloud PLY degrades gracefully, checks a zero-vertex PLY and two structurally-corrupt files are each rejected with a specific on-topic error and no object created, unregisters cleanly -- 36/36 assertions pass as of this run (see blender_addon/README.md "How this was tested" for the full list and one caveat worth knowing: bpy.ops itself turns an operator's reported ERROR into a Python RuntimeError, which the test accounts for). A prior draft of this row claimed an 8-assertion self-test had already caught a real __init__.py import-shadowing bug, but selftest.py did not actually exist on disk when that claim was checked -- unverifiable, so it has been corrected: selftest.py exists now, was actually executed, and the numbers above are from that real run, not carried forward. .spz decode is a best-effort implementation of the publicly documented container format (gzip + fixed header + quantised fields); NOT verified against a byte-exact real .spz sample (none existed to test against) -- flagged PARTIAL, prefer .ply, see spz_reader.py docstring + TODO(nimbus). docs/DATA_FORMAT.md still does not exist on disk (confirmed again this run), so the PLY property-name convention (splat_data.py) follows the de-facto INRIA/gsplat standard; reconciling against our own exporter (if it differs) is confined to a few named constants in that one file. No macOS/CI available here to test the add-on's zip-install path itself, only its Blender-Python behavior once loaded. |
| booster-client (ios/Sources/Booster/) | REAL (discovery/pairing/chunked resumable upload+download/live progress/tab UI) | Pure Swift, no third-party deps, all one Xcode target so no cross-module import needed. Bonjour discovery via NWBrowser + on-demand NWConnection resolve to literal host:port (BrandConfig.boosterServiceType/boosterServiceDomain); code-confirmation pairing (POST /v1/pair/requests -> PC shows a short code -> phone types it in -> token stored in Keychain via a self-contained BoosterKeychain, keyed by BrandConfig.keychainService); manifest-driven chunked (1 MiB) upload with true resume (server-authoritative per-file byte offset, not a local resume-state file, so it survives an app kill or reinstall as long as the Booster still has the job) with SHA-256 per file (CryptoKit, streamed, never loads a whole file into memory) and per-chunk retry with backoff; live progress over a WebSocket (URLSessionWebSocketTask) with automatic reconnect and a polling fallback if the socket never connects; result download via standard HTTP Range requests, also chunked and resumable, with a whole-file checksum verify before it's trusted, landing in Documents/<brand>/Scans/<scanID>/model (BrandConfig.Folder.model); BoosterTabView (SwiftUI, iOS 17) covers found/paired/unreachable devices, the pairing-code sheet, a scan picker, one live progress section, and a results history persisted to Documents/<brand>/cache/booster_state.json. Every user-facing error string is plain language (no jargon), per the owner's stated preference. SECOND PASS (2026-09-03): architect again produced nothing (CONTRACTS.md, Core/Contracts.swift, docs/BOOSTER_PROTOCOL.md, docs/DATA_FORMAT.md were still absent from disk at that point), so that run re-verified the existing ~2860 lines line-by-line against real Foundation/Network/CryptoKit/SwiftUI/os signatures instead of rewriting, and fixed three real bugs found on inspection: (1) BoosterJobRecord.createdAt was being re-stamped with `Date()` on every progress event, silently corrupting the Results list's "created" ordering into "last updated" order -- now captured once per job and threaded through every upsertJob call; (2) BoosterProgressSection rendered a determinate progress bar pinned at 0% during stages with no fractionComplete (queued/verifying/exporting), which reads as frozen to a non-technical user -- now shows an indeterminate spinner instead whenever the fraction is genuinely unknown; (3) the HTTP-426 apiVersionMismatch error hardcoded "Booster is on unknown" -- now reads the Booster's own X-Nimbus-Api-Version response header when present. THIRD PASS (2026-09-03, same day): CONTRACTS.md, Core/Contracts.swift, docs/BOOSTER_PROTOCOL.md and docs/DATA_FORMAT.md now exist -- this pass read all four in full and reconciled against them rather than reinventing. Result: docs/BOOSTER_PROTOCOL.md was reverse-specified from this module's own code, so every DTO field name, enum spelling, endpoint path, status-code mapping, resume/reconnect timing (0/1/2/5/10/20 s WebSocket backoff summing to the doc's "~38 seconds", 3 s poll, 3-try/0.5s-1s chunk retry) and encoding rule (.iso8601 no-fractional-seconds dates, server-authoritative receivedByteOffset, chunkSize ignored and hardcoded to 1 MiB) already matched byte-for-byte on inspection -- zero drift, confirmed by reading BoosterProtocol/HTTP/Discovery/Pairing/Upload/Download/JobMonitor/JobStore/ManifestBuilder/ScanLister against the doc line by line. `BoosterClient` conforms to Core's `BoosterService` (name divergence per CONTRACTS.md section 6.2: `BoosterClient` already the concrete class) via the extension in App/NimbusApp.swift, which maps `resultsHistory() -> [BoosterJobRecord]` to the protocol's `jobs() -> [BoosterJob]`; no change needed on this side since the two record types are already field-identical per CONTRACTS.md 6.4. Fixed two real bugs found on this reconciliation pass, neither previously caught: (1) `BoosterEndpointAddress.url(path:)` built every request URL via `URLComponents.path = path`, but `BoosterAPI.jobFile`/`resultFile` had already percent-encoded the relative path with `.urlPathAllowed` before handing it in -- `URLComponents.path`'s setter treats its input as unencoded and re-escapes it during composition, so any relative path actually needing escaping (a filename with a space or non-ASCII character) would double-encode (`%20` -> `%2520`) and 404 against the server; switched to `.percentEncodedPath`, which composes the already-encoded string as-is. Latent, not yet observed, because every filename this app's own capture pipeline writes (timestamp + underscores, per docs/DATA_FORMAT.md) happens to need no escaping at all -- but a real bug regardless, and the fix is the kind of thing a byte-exact protocol doc is supposed to make possible to catch. (2) `BoosterManifestBuilder.buildManifest` (streaming SHA-256 of every file in the scan folder, potentially several GB on a whole-house scan) was called directly from `BoosterClient.runSendScan`, a method of a `@MainActor` class -- meaning every "Send a scan" tap hashed gigabytes of data on the main thread and would visibly freeze the Booster tab's UI for the whole duration. Moved manifest-building inside `BoosterUploadManager.upload(scanID:scanDirectory:...)`, which is a plain (non-`@MainActor`) `actor`, so the hashing now runs on its own executor; `BoosterClient` no longer builds the manifest itself, it just passes `scanID`/`scanDirectory` through. No wire-format change, no caller outside this module. Also swept every file's header comment for the now-stale "CONTRACTS.md/Contracts.swift do not exist on disk" honesty notes from the first two passes (BoosterProtocol.swift, BoosterClient.swift, BoosterKeychain.swift, BoosterManifestBuilder.swift, BoosterScanLister.swift) and rewrote them to point at CONTRACTS.md's real section numbers instead of the stale "absent from disk" claim, since leaving them would mislead the next reader/agent into re-litigating an already-settled question. REMAINING HONEST GAPS: the wire protocol has still only been checked against the doc it was reverse-specified from, never against a real running Python `booster/` server (none exists on this machine to test against) -- if `booster/` lands with a different shape than the doc, this module's contact surface is exactly `BoosterProtocol.swift` + `BoosterHTTP.swift`, everything else talks to the network only through those two. `BoosterScanLister.swift` still reads scan folders directly off disk (per CONTRACTS.md 6.5, correct until Sources/Viewer's real library type exists). No macOS/Xcode here, so nothing in this module has been compiled this pass either -- the two fixes above were verified by re-reading the real Foundation/URLComponents and Swift actor-isolation semantics they depend on, not by building. |
| export (ios/Sources/Export/) | REAL (.ply write+read, .spz v1-v3 write+read, KHR_gaussian_splatting .glb write, ZIP capture-bundle packaging, share sheet) / honest STUB (.spz v4 read only) | Pure Swift, no third-party deps, one Xcode target. SplatCloud.swift defines this module's own splat contract (positions/rotations/log-scale/opacity-logit/raw SH coefficients, RUB frame) since Core/Contracts.swift does not exist on disk -- TODO(nimbus) comment marks it for folding into Core once that lands. PLYCodec: the INRIA/gsplat-standard vertex-only float PLY layout (x y z, nx ny nz, f_dc_0-2, f_rest_*, opacity, scale_0-2, rot_0-3), verified property-by-property against Niantic's own C++ reference (github.com/nianticlabs/spz src/cc/load-spz.cc, fetched live via `gh api` on 2026-09-03, not from memory); binary_little_endian write, binary_little_endian + ascii read; RUB<->RDF coordinate flip on position/rotation only. SPZCodec: a real, from-scratch gzip (RFC 1952) container -- Apple's Compression framework only emits raw DEFLATE (RFC 1951, confirmed via Apple's own deflateInit2(windowBits:-15) equivalence note), so GzipCodec.swift hand-wraps it with a real 10-byte header + CRC32 (table-driven, own implementation, verified against the standard "123456789"->0xCBF43926 check vector) + ISIZE trailer, byte-for-byte valid gzip, decompressible by any gzip tool; writes SPZ version 3 (24-bit fixed-point positions, "smallest three" quaternion compression, per-band SH quantization matching the reference's DEFAULT_SH1_BITS/DEFAULT_SH_REST_BITS exactly) rather than version 2, a deliberate choice after measuring v2's "first three" encoding blow up (>0.1 single-component error observed) whenever the true w is near zero, vs. v3's stable ~0.002 typical error; reads versions 1-3. Version 4 (NGSP, Zstandard multi-stream) is honestly rejected with a typed, specific error (SPZError.unsupportedContainerVersion4) explaining exactly why (no ZSTD in Apple's Compression framework, project.yml packages:{} forbids an external dependency) rather than silently mis-decoding it -- this is the one deliberately-out-of-scope piece, flagged per the honesty contract. GLTFExporter: KHR_gaussian_splatting, verified against the RATIFIED Khronos spec (status "Complete, Ratified by the Khronos Group", fetched and read in full from github.com/KhronosGroup/glTF on 2026-09-03) -- correct attribute semantics (POSITION + KHR_gaussian_splatting:ROTATION/SCALE/OPACITY/SH_DEGREE_n_COEF_m), POINTS primitive mode, scale/opacity correctly activated (exp/sigmoid) from SplatCloud's raw log/logit storage since glTF wants linear values unlike PLY/SPZ, no coordinate flip needed since glTF's RUB convention already matches SplatCloud's. ZipWriter: a from-scratch PKZIP writer (local headers + central directory + EOCD), STORE only (no DEFLATE, no Zip64 -- deliberate scope choice for determinism/correctness given no compiler was available to test against), streams every entry from disk in 4 MiB chunks rather than loading a whole capture bundle into memory. BoosterBundle zips a scan's images/sensor_data/sparse/anchors/mesh/prepass folders (BrandConfig.Folder names) plus a small manifest.json, since neither docs/DATA_FORMAT.md nor docs/BOOSTER_PROTOCOL.md exist yet to pin the exact contract -- INTEGRATION NOTE: booster-client's own row above describes a manifest-driven PER-FILE chunked upload, which may be a different transport shape than "send one zip"; these two have not been reconciled and may need one to defer to the other once docs/BOOSTER_PROTOCOL.md exists. ExportService is the module's facade (export/import/package/share-sheet) plus an ExportServicing protocol proposed as a Core/Contracts.swift candidate. VERIFICATION: no macOS/Xcode here, so nothing in this module has been compiled. Every wire-format byte layout was fetched from and cross-checked against real upstream source (Niantic's C++ SPZ implementation, the ratified Khronos KHR_gaussian_splatting spec) rather than recalled from memory, and every non-trivial numeric algorithm (24-bit fixed-point position round-trip, both v2 "first three" and v3 "smallest three" quaternion pack/unpack, SH quantization buckets, the gzip container's byte layout) was independently validated with standalone Python prototypes cross-checked against Python's real zlib/gzip before being ported to Swift. Defensive NaN/Inf guards were added at every trapping Float->fixed-width-integer conversion (quaternion pack, position pack, SH quantize, UInt8 clamp) so one corrupt/diverged splat cannot crash a whole export. In lieu of an XCTest target (project.yml defines only the App target; adding a test target is a shared, cross-module build-config change out of this module's "own directory only" mandate), ExportSelfTest.swift is a DEBUG-only runtime self-check harness (round-trip and known-answer tests, incl. the CRC32 check vector, PLY bit-exact round trip, SPZ tolerance-bounded round trip, GLB container structural validation) callable via ExportSelfTest.runAll() -- TODO(nimbus) notes porting it into a real XCTest target once one exists. SECOND PASS (2026-09-03, same day): CONTRACTS.md, Core/Contracts.swift, docs/DATA_FORMAT.md, docs/BOOSTER_PROTOCOL.md are still absent from disk (re-confirmed), so no reconciliation was possible; instead this pass re-verified the prior claims for real rather than trusting them, by pulling the actual upstream sources live with `gh api` and diffing this module's math against them line-by-line: nianticlabs/spz's src/cc/load-spz.cc + splat-utils.h + load-spz.h (magic 0x5053474e, 16-byte legacy header layout, 12 fractional bits, DEFAULT_SH1_BITS=5/DEFAULT_SH_REST_BITS=4, colorScale=0.15, sqrt1_2, the quantizeSH round-then-bucket formula, the scale/alpha/color/position packing formulas, and both the pack and unpack "smallest three" quaternion bit orderings) all matched this codec's Swift byte-for-byte, and KhronosGroup/glTF's ratified KHR_gaussian_splatting README (status confirmed "Complete, Ratified by the Khronos Group") confirmed every attribute semantic name, the mode=0/POINTS requirement, and the exact extension-object property values ("ellipse"/"srgb_rec709_display"/"perspective"/"cameraDistance") against the spec's own worked JSON example. That same README's "Fallback Behavior" section surfaced one genuine gap: the ratified spec explicitly recommends a core (non-namespaced) `COLOR_0` VEC4 accessor -- RGB from the degree-0 SH coefficient decoded sRGB-to-linear, alpha = opacity -- so a plain glTF/point-cloud viewer that does not understand KHR_gaussian_splatting at all still renders a correctly colored point cloud instead of black dots; GLTFExporter.swift now writes it (new `srgbDisplayToLinear` IEC 61966-2-1 helper), and ExportSelfTest's GLB structural check now asserts the primitive's attributes include COLOR_0. No other correctness issues found on this pass; no macOS/Xcode here so still nothing in this module has been compiled. THIRD PASS (2026-09-03, same day, after CONTRACTS.md, Core/Contracts.swift, docs/DATA_FORMAT.md and docs/BOOSTER_PROTOCOL.md landed from the architect): read all four in full and diffed this module's existing code against them line-by-line rather than rewriting, per this module's own "improve, don't restart" mandate. Result: CONTRACTS.md section 6 states plainly that nothing here was required to change for the build to work, and that held up -- `SplatCloud`/`SHDegree`/`ExportError`/`ExportFormat` are promoted in place exactly as this module already declared them (confirmed `SHDegree` here still does NOT declare `Codable`, since Core's `Contracts.swift` line 1246 owns that single conformance and a second one would be a hard duplicate-conformance compile error); `SplatExporting`'s three methods (`exportAsset`/`packageCaptureBundle`/`importSplatCloud`) are already satisfied by `Sources/App/NimbusApp.swift`'s bridging extension over the concrete `export`/`packageForBooster`/`importPLY`/`importSPZ` methods this module exposes, so App's file did not need touching and wasn't. Two real, small fixes made this pass, both against docs/DATA_FORMAT.md section 1's directory layout, which is normative: (1) the capture-bundle zip was named `<scanID>-booster.zip`; DATA_FORMAT.md's `export/` listing fixes the name as `<scanID>-capture-bundle.zip`, so `ExportService.packageForBooster` now writes that name. (2) `BoosterBundle.swift` zipped the `images/sensor_data/sparse/anchors/mesh/prepass` folders but never the scan-root `capture_bundle.json` index file that section 1 lists first and that every sidecar joins against by filename -- a zip without it is not self-describing to anything that doesn't already know this app's internal layout. Fixed: it now copies `capture_bundle.json` into the archive root when present (tolerantly skipped if genuinely absent, e.g. a capture that crashed before it was written -- never a hard failure over one optional file). Also reconciled docs/BOOSTER_PROTOCOL.md section 13 confirms explicitly, in writing, the same zip-vs-chunked-transport split this module's prior passes flagged as an open question with Booster's row above -- it is now settled and requires no code change (packageForBooster's method name stays as-is; CONTRACTS.md section 6.3 confirms the Core-facing name via `SplatExporting.packageCaptureBundle` is the only name that had to be right and it already is). Added two new ExportSelfTest checks exercising the fix for real (build a fixture scan dir with a `capture_bundle.json` and section folders under `FileManager.default.temporaryDirectory`, package it, assert the root file and a section file both appear in the zip's raw bytes, assert the ZIP's End-Of-Central-Directory signature is present near the tail) rather than just asserting the change compiles. GLTFExporter/PLYCodec/SPZCodec/ZipWriter/GzipCodec/CRC32/BinaryIO were re-read against Contracts.swift's coordinate-convention section (world Y-up, camera +Y-down/+Z-forward, the one `Pose.fromARKitCameraTransform` conversion, RUB splat storage, PLY's RDF flip) and found already consistent -- no other changes needed. Still no macOS/Xcode here, so still nothing in this module has been compiled; every change this pass was checked by hand against the four newly-landed documents plus this module's own existing (previously upstream-verified) logic. |
| core+app-shell (ios/Sources/Core/, ios/Sources/App/, ios/project.yml, CONTRACTS.md, docs/DATA_FORMAT.md, docs/BOOSTER_PROTOCOL.md) | REAL (contracts, brand config, app shell, format + protocol specs) / by-design PLACEHOLDER (Capture and Scans screens) | Contracts.swift is the shared vocabulary for the whole pipeline: Codable/Sendable geometry (Vector3/Quaternion/Pose/BoundingBox/CameraIntrinsics with ONE ARKit->COLMAP conversion in Pose.fromARKitCameraTransform, world->camera, +Y down +Z forward), CaptureBundle/CaptureFrame/FrameQC/AnchorRecord/MeshChunkRef/RevisitPair/CaptureSettings, PrePassResult with OccupancyState(unknown/empty/surface), TrustFieldRefs (coarse bias field + never-averaged per-sample noise field kept as separate files, deliberately), EdgeClass(none/geometric/texture/unknown/band), GlassRegion, InitialSplatSetRef, QCCard/QCFinding; DeviceTier + DeviceCapabilityReport + FeatureAvailability; TrainingBudget with a real (not stubbed) recommended(for:sceneExtentMeters:availableMemoryBytes:) that returns the spec's own ranges; SplatModel, TrainerStage/TrainerProgress, PreviewCameraPath, ExportedAsset, BoosterJob, NimbusError; and the twelve service protocols with exact async signatures (AsyncThrowingStream for PrePass and Trainer, @MainActor on Capture/Renderer/Booster). WRITTEN AGAINST THE TWO MODULES THAT ALREADY EXISTED, not around them: ios/Sources/Export and ios/Sources/Booster were read line by line first, and because the app is ONE Xcode target (no namespacing, a duplicate top-level type name is a hard compile error) Core deliberately does NOT re-declare SplatCloud, SHDegree, ExportError, ExportFormat, or any of Booster's wire types -- it references them in place and CONTRACTS.md section 6 lists every promoted type. Two protocol names had to diverge from the brief to avoid colliding with shipped concrete classes: SplatExporting (not ExportService, which is Export's class, and not ExportServicing, which is Export's own protocol) and BoosterService (not BoosterClient, which is Booster's class); both existing classes are made to conform by short extensions in NimbusApp.swift, so nothing was renamed, deleted or moved in another agent's directory. Core owns one added conformance, `extension SHDegree: Codable {}` (SHDegree ships without it and TrainingBudget/SplatModel both serialise); Export must not also declare it. Settled the one real open question the two modules flagged at each other: the Booster transport is Booster's per-file chunked resumable manifest upload, NOT Export's single zip (a zip cannot resume, doubles peak disk on a house scan, and cannot be verified before unpacking); Export's zip keeps the separate AirDrop/manual -transfer job and its reconciliation is a rename only. project.yml was already written and was kept intact (brand block, iOS 17, iPhone-only, Metal, entitlements, Bonjour, UIRequiredDeviceCapabilities all correct); the one change was marking the six not-yet-existing module source dirs `optional: true`, because XcodeGen hard-errors on a missing source path and `xcodegen generate` would otherwise have failed for every agent until all eleven modules landed. docs/BOOSTER_PROTOCOL.md was reverse-specified from the real Swift client (BoosterProtocol/HTTP/Discovery/Pairing/Upload/Download/JobMonitor) rather than invented, so the Python server has something byte-exact to target: endpoints, the X-Nimbus-* headers, jobs-keyed-by-scanID resume, server-authoritative receivedByteOffset, the WebSocket + 3 s polling fallback, Range download semantics, and the status-code table are all what the client actually does -- including three traps a tidier imaginary spec would get wrong (Foundation's .iso8601 rejects fractional seconds; the client ignores the server's advertised chunkSize and always sends 1 MiB; .urlPathAllowed does not escape `/`, so the files route must be a wildcard). docs/DATA_FORMAT.md fixes every filename and byte layout and is compatible with what already shipped: the frame stamp matches Booster's own example path, the folder names are BrandConfig.Folder's, and the PLY property convention matches Export's PLYCodec AND the guess blender_addon/splat_data.py already made (verified against PLYCodec: rot_0..3 = w,x,y,z, log scales, logit opacity, channel-major f_rest_* -- the add-on guessed right, no change needed there). NimbusApp.swift is a real, thin shell: @main, a compat gate driven by the Core protocol, a TabView, and an integration block where a module is wired in with one uncommented line; the Booster tab is REAL (it embeds the shipped BoosterTabView) and Export is really registered, while Capture and Scans render honest "Not built yet, this screen comes from the X module" placeholders rather than fake UI. HONEST LIMITS: there is no macOS or Xcode on this machine, so NOTHING here has been compiled -- the Swift was written against real Foundation/simd/SwiftUI signatures and checked by hand, and two spots carry known compile-time assumptions worth naming: (a) SwiftUI's `App` protocol is @MainActor in the iOS 17 SDK, which is what lets NimbusApp.init() call the MainActor-isolated bootstrap; (b) the stdlib's RawRepresentable-where-RawValue==Int Codable defaults are what make the empty `extension SHDegree: Codable {}` a complete conformance. Both are standard, both are the first things to check if CI reports an error in these files. Nothing in Core is a stub: every type and every protocol is concrete and the two pieces of real logic in it (the ARKit pose conversion and TrainingBudget.recommended) are implemented, not placeheld. |
